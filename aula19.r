#Aula19 - Soma de valores da matriz

sum(as.integer (nomeMatriz[,]))
#É necessário converter os valores que precisam ser somados

#Atribuindo a soma à matriz e atualizando valores

nomeMatriz <- sum(as.integer(nomeMatriz[,]))